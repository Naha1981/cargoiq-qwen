services:
  - type: web
    name: cargoiq-worker
    runtime: node
    buildCommand: cd render-worker && npm install
    startCommand: cd render-worker && npm start
    plan: free
    envVars:
      - key: PORT
        value: "3001"
